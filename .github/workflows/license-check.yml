name: License Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  license-check:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: |
            cli/package-lock.json
            agenticflow-memory-mcp/package-lock.json

      - name: Check CLI Licenses
        run: |
          cd cli
          npm ci
          npx license-checker --production --onlyAllow 'MIT;Apache-2.0;BSD-2-Clause;BSD-3-Clause;ISC;0BSD' --excludePrivatePackages

      - name: Check Memory MCP Licenses
        run: |
          cd agenticflow-memory-mcp
          npm ci
          npx license-checker --production --onlyAllow 'MIT;Apache-2.0;BSD-2-Clause;BSD-3-Clause;ISC;0BSD' --exclude "@chroma-core/ai-embeddings-common" --excludePrivatePackages
