# Changelog

All notable changes to this project will be documented in this file.

## [1.0.0] - Initial Extraction
### Added
- Extracted and decoupled `local`, `ollama`, and `openai` embedding providers from `agenticflow-memory-mcp`.
- Standardized the provider initialization to accept a configuration object rather than relying on global environment variables.
