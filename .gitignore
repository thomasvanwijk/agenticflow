# Personal config — never commit these
.env
config/config.yaml
config/servers.yaml
# servers.d/*.json files only contain ${VAR} placeholders — safe to commit
config/secrets.enc

# Node
node_modules/
dist/
*.log

# Python
__pycache__/
*.pyc
.venv/

# Docker volumes (if mounted locally)
data/

# OS
.DS_Store
Thumbs.db

# don't commit research files
Research/
# AI context and agent files
GEMINI.md
.gemini/
docs/ROADMAP_EVALUATION.md
docs/ROADMAP.md
